;;; config-lang-thrift.el -- Emacs config for thrift.
;;; Commentary:
;;; Code:


(use-package thrift
  :ensure t
  :mode "\\.thrift\\'")

(provide 'config-lang-thrift)
;;; config-lang-thrift.el ends here
